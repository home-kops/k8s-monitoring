apiVersion: v1
kind: Service
metadata:
  name: {{ include "grafana-mod.fullname" . }}
  labels:
    {{- include "grafana-mod.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "grafana-mod.selectorLabels" . | nindent 4 }}
