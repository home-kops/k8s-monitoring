apiVersion: apps/v1
kind: Deployment
metadata:
  name: suricata
  namespace: monitoring
spec:
  strategy:
    type: Recreate
  selector:
    matchLabels:
      app: suricata
  template:
    metadata:
      labels:
        app: suricata
    spec:
      hostNetwork: true
      containers:
      - name: suricata
        image: jasonish/suricata:7.0.10
        securityContext:
          capabilities:
            add: ["NET_ADMIN", "SYS_NICE"]
        volumeMounts:
        - name: hostdev
          mountPath: /dev
        - name: config
          mountPath: /suricata.yaml
          subPath: suricata.yaml
          readOnly: true
        - name: log
          mountPath: /var/log/suricata
        command: ["/usr/bin/suricata", "-i", "eth0", "-c", "/suricata.yaml", "-v"]
      volumes:
      - name: hostdev
        hostPath:
          path: /dev
      - name: config
        configMap:
          name: suricata-config
      - name: log
        persistentVolumeClaim:
          claimName: suricata-logs
      tolerations:
      - key: "role"
        operator: "Equal"
        value: "node-m"
        effect: "NoSchedule"
